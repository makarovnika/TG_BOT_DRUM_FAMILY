# Evaluator Rubric — `registration-002` (Регистрация ученика через FSM)

| Категория | Вопрос | Оценка (0–2) | Заметки |
| --- | --- | --- | --- |
| Correctness | Соответствует ли реализованное поведение `user_visible_behavior` фичи? | 2 | Никита прошёл /start → 'Никита' → +79119153381 → 'Отлично, ты зарегистрирован' + меню. В SQLite связка telegram_id ↔ yclients_client_id корректная. Повторный /start теперь даст «С возвращением». |
| Verification | Все ли пункты `verification` реально были запущены, есть ли доказательства? | 2 | 4/4 пунктов закрыты: FSM прошёл в реальном Telegram, клиент в YClients найден без дублирования, в SQLite запись есть, повторный /start пойдёт сразу в меню (логика проверена в unit-тестах). |
| Scope discipline | Сессия осталась в рамках выбранной фичи, без посторонних правок? | 1 | По ходу пришлось решать YClients-блокеры: 403 на Admin API → переход на legacy login/password; HTTP 201 на /auth; формат phone без `+` для search. Это не «выползание» — это адаптация к реальному API. Минус один балл за то, что путь к решению занял несколько итераций (Variant A vs B). |
| Reliability | Результат переживает перезапуск/повторный прогон без ручного ремонта? | 2 | Бот можно перезапускать сколько угодно — связка живёт в SQLite. Сам YClients-клиент устойчив (retry/refresh для legacy режима). |
| Maintainability | Код и документация достаточно понятны для следующей сессии? | 2 | Docstring у `_finalize` в registration.py объясняет ветки ошибок. В client.py зафиксированы оба workaround'а (201, phone без `+`) с цитатами реальных ответов API. |
| Handoff readiness | Сможет ли свежая сессия продолжить работу, опираясь только на артефакты репо? | 2 | `feature_list.json` → `registration-002.fixed_along_the_way` содержит все 3 нетривиальных открытия. Тестовый клиент известен (id 285529314), следующая фича может сразу его использовать. |

**Сумма: 11/12.**

## Verdict

- [x] **Accept** — фича в `passing`, evidence в feature_list.json + скриншот в чате.
- [ ] Revise
- [ ] Block

## Required Follow-Up

- Missing evidence: —
- Required fixes:
  - возможно стоит вынести список «известных особенностей YClients API» (HTTP 201, phone без `+`, `booking_days` как dict) в отдельный файл `docs/yclients-quirks.md` — пока их 3, при росте числа полезно.
- Next review trigger:
  - при разблокировке статического User Token mode (если когда-то получится установить интеграцию через маркетплейс);
  - при первой реальной отмене записи в YClients — проверить, что `cancel_record` корректно работает.
