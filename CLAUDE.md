# CLAUDE.md — Telegram-бот для школы барабанов

Ты работаешь в репозитории, рассчитанном на длительную разработку с участием
агента. Главное — надёжное завершение фич, непрерывность между сессиями и явная
проверка результата, а не скорость генерации кода.

Заказчик — **Никита**, уровень Python — базовый. Объясняй:
- **что** делаем на этом шаге,
- **почему** именно так, а не иначе,
- **что проверить**, чтобы убедиться, что работает,
- любые **подводные камни**. По-русски.

---

## Контекст проекта

Школа барабанов: 3 преподавателя, 5 залов, ~30–100 учеников.
YClients уже используется как CRM (расписание, записи, оплаты, абонементы).
Цель — сделать удобный Telegram-бот поверх YClients для учеников.

**Текущий этап:** MVP без n8n, без финансов, без Notion. Только бот + YClients API.

---

## Operating Loop (обязательно в начале каждой сессии)

1. Выполни `pwd` и убедись, что находишься в корне репозитория.
2. Прочитай `claude-progress.md` — последнее подтверждённое состояние и следующий шаг.
3. Прочитай `feature_list.json` — выбери фичу с наивысшим приоритетом и статусом `not_started` или `in_progress`.
4. Посмотри последние коммиты: `git log --oneline -5`.
5. Запусти `./init.sh`.
6. Проверь, что базовая верификация (smoke) проходит. Если нет — чини её первой.

Затем выбери **ровно одну** незавершённую фичу и работай только над ней,
пока не подтвердишь её или не задокументируешь блокер.

---

## Архитектура

```
Telegram-бот (Python 3.11+, aiogram 3.x)
    │
    ├─▶ YClients API (источник правды по расписанию и записям)
    │
    └─▶ SQLite (только маппинг Telegram_ID ↔ YClients_client_id + FSM-состояния)
```

## Стек

| Что | Чем | Почему |
|-----|-----|--------|
| Язык | Python 3.11+ | Базовый опыт у заказчика |
| Бот-фреймворк | aiogram 3.x | Современный, асинхронный |
| HTTP-клиент | httpx | Асинхронный, удобный для API |
| База | SQLite + SQLAlchemy 2.x (async) | Для 100 учеников хватит |
| Конфиг | pydantic-settings + .env | Безопасно для токенов |
| Логи | structlog | Читаемые JSON-логи |
| Тесты | pytest + pytest-asyncio + respx | Моки YClients API |
| Линтер | ruff | Один инструмент вместо black+flake8+isort |
| Менеджер зависимостей | uv | Быстрый, современный |

## Структура проекта (целевая)

```
.
├── CLAUDE.md                  ← этот файл
├── AGENTS.md                  ← правила работы агента
├── README.md                  ← инструкция по запуску
├── init.sh                    ← стандартный путь старта + проверки
├── claude-progress.md         ← журнал сессий
├── feature_list.json          ← источник правды по статусу фич
├── session-handoff.md         ← компактные заметки для следующей сессии
├── clean-state-checklist.md   ← чек-лист окончания сессии
├── evaluator-rubric.md        ← рубрика проверки фичи
├── quality-document.md        ← снимок качества по доменам
├── pyproject.toml             ← зависимости (uv)
├── .env.example
├── .env                       ← реальные токены (в .gitignore!)
├── .gitignore
├── docker-compose.yml
├── Dockerfile
├── src/
│   ├── main.py
│   ├── config.py
│   ├── bot/{handlers,keyboards,states,middlewares}/
│   ├── yclients/{client,models,exceptions}.py
│   ├── db/{models,session}.py
│   └── services/{user_service,booking_service}.py
└── tests/
    ├── conftest.py
    ├── test_yclients_client.py
    └── test_handlers.py
```

---

## Правила работы

- **Одна активная фича за раз.** Не начинай вторую, пока первая не в статусе `passing` или `blocked`.
- **Не помечай фичу `passing`, если код просто написан** — нужна реально прошедшая верификация и зафиксированные доказательства.
- **Не переписывай feature_list.json, чтобы скрыть незавершённую работу.**
- **Не ослабляй тесты, чтобы фича «выглядела готовой».**
- **Не меняй правила верификации молча по ходу разработки.**
- Repository artifacts — единственный источник правды (не чат).

## Definition Of Done

Фича считается завершённой, только если **все** условия выполнены:
- целевое поведение реализовано,
- требуемая верификация **реально запускалась**,
- доказательства зафиксированы в `feature_list.json` или `claude-progress.md`,
- репозиторий остаётся запускаемым через `./init.sh`.

---

## Принципы кода

1. **Async везде.** aiogram, httpx, SQLAlchemy — всё асинхронное.
2. **Типизация.** Все функции с type hints, pydantic-модели для API.
3. **Без god-классов.** Бизнес-логика в `services/`, обработчики тонкие — только UI.
4. **Обработка ошибок YClients:**
   - 401 → перевыпустить user_token
   - 429 → retry с backoff
   - 5xx → 3 попытки, потом сообщить пользователю «сервис временно недоступен»
5. **Логи через structlog**, JSON-формат.
6. **Секреты только через .env.** Никаких хардкодов токенов.
7. **Тесты:** мокать YClients API через `respx`, не ходить в реальный сервис.

---

## YClients API — ключевые моменты

**Базовый URL:** `https://api.yclients.com/api/v1/`

**Авторизация:** два токена в одном заголовке:
```
Authorization: Bearer {partner_token}, User {user_token}
Accept: application/vnd.api.v2+json
```

`partner_token` — постоянный, с developers.yclients.com
`user_token` — получают через `POST /auth`, нужно обновлять при 401

**Подводные камни:**
- Метод `book_record` обычно требует SMS-подтверждения (`book_code`). Перед стартом
  Шага 3 (запись на индивидуальное) уточнить у поддержки YClients: можно ли отключить
  SMS для записей через бота или использовать запись от имени админа.
- Лимит: ~200 запросов/мин с одного токена. Кэшировать список услуг и преподавателей
  в памяти на 5 минут.
- Все datetime — Unix timestamp в UTC.

**Документация:**
- https://developers.yclients.com/ru/
- https://yclients.docs.apiary.io/

---

## Перед завершением сессии

1. Обнови `claude-progress.md` (добавь запись новой сессии).
2. Обнови `feature_list.json` (новый статус, evidence).
3. Заполни `session-handoff.md` (что готово, что сломано, следующий шаг).
4. Закоммить, когда репозиторий в безопасном состоянии.
5. Оставь путь восстановления: следующая сессия должна сразу запустить `./init.sh`.

См. также `clean-state-checklist.md`.

---

## Что НЕ делаем на этом этапе

- ❌ Оплаты (ЮKassa) — позже, через n8n
- ❌ Синхронизация в Google Sheets — позже, через n8n
- ❌ Notion-интеграция для клиентских заметок — позже
- ❌ Уведомления и напоминания — позже, через n8n
- ❌ Админ-панель — позже
- ❌ Видео-материалы и домашка — позже
